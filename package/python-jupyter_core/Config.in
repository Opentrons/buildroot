config BR2_PACKAGE_PYTHON_JUPYTER_CORE
	bool "python-jupyter_core"
	select BR2_PACKAGE_PYTHON_TRAITLETS # runtime
	help
	  Jupyter core package. A base package on which Jupyter
	  projects rely.

	  https://jupyter.org

config BR2_PACKAGE_PYTHON_JUPYTER_CORE_TESTS
	bool "jupyter_core.tests"
	help
		Include test files.
		Selecting this option adds about 0.5 MB to the target file
		system.
