config BR2_PACKAGE_PYTHON_PANDAS
	bool "python-pandas"
	select BR2_PACKAGE_PYTHON_DATEUTIL # runtime
	select BR2_PACKAGE_PYTHON_PYTZ # runtime
	select BR2_PACKAGE_PYTHON_NUMPY # runtime
	select BR2_PACKAGE_PYTHON_TZDATA # runtime
	select BR2_PACKAGE_PYTHON3_BZIP2 # runtime
	select BR2_PACKAGE_HOST_PYTHON_MESON # build time for headers
	select BR2_PACKAGE_HOST_PYTHON_PYPROJECT_METADATA # build time for headers
	select BR2_PACKAGE_HOST_PYTHON_WHEEL # build time for headers
	select BR2_PACKAGE_HOST_PYTHON_VERSIONEER # build time for headers
	select BR2_PACKAGE_HOST_PYTHON_OLDESET_SUPPORTED_NUMPY # build time for headers
	select BR2_PACKAGE_HOST_PYTHON_NUMPY # build time for headers
	select BR2_PACKAGE_HOST_PYTHON_CYTHON  # build time for cython compilation
	help
	  Powerful data structures for data analysis, time series,
	  and statistics.

	  http://pandas.pydata.org

config BR2_PACKAGE_PYTHON_PANDAS_TESTS
	bool "pandas.tests"
	help
		Include test files.
		Selecting this option adds about 7.0 MB to the target file
		system.
