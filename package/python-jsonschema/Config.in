config BR2_PACKAGE_PYTHON_JSONSCHEMA
	bool "python-jsonschema"
	help
	  An implementation of JSON Schema validation for Python.

	  http://github.com/Julian/jsonschema

config BR2_PACKAGE_PYTHON_JSONSCHEMA_TESTS
	bool "jsonschema.tests"
	help
		Include test files.
		Selecting this option adds about 0.5 MB to the target file
		system.
