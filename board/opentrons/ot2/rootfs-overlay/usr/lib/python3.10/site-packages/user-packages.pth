# We've configured pip so that when a user SSHes in and does `pip install foo`,
# pip installs the package in a custom path.
# This file makes sure Python knows to look for packages there.

/var/user-packages/usr/lib/python3.10/site-packages
